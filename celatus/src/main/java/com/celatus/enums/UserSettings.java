package com.celatus.enums;

/**
 * Defines all of the settings read and written by our application through the
 * {@link com.celatus.handler.PropertyHandler} class
 */
public enum UserSettings {
    THEME,
    PASSWORDS_VISIBLE
}
